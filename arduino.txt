#include <Servo.h>
int x;
Servo myservo;
int val;
void setup() {
	Serial.begin(115200);
	Serial.setTimeout(1);
  myservo.attach(9);
}
void loop() {
	while (!Serial.available());
	x = Serial.readString().toInt();
  val = x;
  myservo.write(x);
	Serial.print(x + 1);
}
